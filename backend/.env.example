# Copy to .env and fill in real values. Never commit the real .env file.
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Only needed if you wire up real email delivery for the contact form.
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# CONTACT_TO_EMAIL=csds23026@glbitm.ac.in
