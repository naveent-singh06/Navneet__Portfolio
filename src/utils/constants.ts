/** Shared, framework-agnostic constants used across the app. */
export const BREAKPOINTS = {
  mobile: 640,
  tablet: 860,
  laptop: 900,
  desktop: 1220,
} as const;

export const NAV_SCROLL_OFFSET = -40;
export const NAV_SCROLL_SPY_OFFSET = 160;
